.grid_12#content#theme_drill_down
  
  .spacer

  - @themes.each do |@theme|
    = render :partial => "theme", :locals => { :theme => @theme }

    .spacer.clear
    .spacer.clear
    .spacer.clear
    
    